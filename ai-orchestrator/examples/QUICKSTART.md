# 快速开始指南

## 5 分钟上手 Claude-Gemini Bridge

> ✅ **好消息！** 所有组件已全局配置完成，你可以在任何项目中使用！

### 前置检查

```bash
# 1. 检查 Gemini CLI 已安装
gemini --version

# 2. 检查全局配置已就位
ls ~/.claude/mcp.json                      # ✅ MCP Server 配置
ls ~/.claude/skills/ai-compare/SKILL.md    # ✅ AI 对比技能
ls ~/.claude/agents/multi-ai-orchestrator/AGENT.md  # ✅ 多 AI 协作器

# 3. 检查 MCP Server 依赖
cd /Users/apple/claudecode/claude-gemini-bridge/gemini-mcp-server
npm install  # 如果还没安装
```

---

## 配置说明（已完成）

所有组件已配置到全局环境，无需额外配置：

| 组件 | 全局位置 | 状态 |
|------|---------|------|
| **MCP Server** | `~/.claude/mcp.json` | ✅ 已配置 |
| **ai-compare Skill** | `~/.claude/skills/ai-compare/` | ✅ 已就位 |
| **multi-ai-orchestrator Agent** | `~/.claude/agents/multi-ai-orchestrator/` | ✅ 已就位 |

**你可以在任何项目目录中直接使用这些功能！**

---

## 测试步骤

### 测试 1: MCP 工具 - 查看模型信息

在 Claude Code 中运行：

```
你: 使用 gemini_model_info 查看可用的 Gemini 模型
```

**预期结果**：应该看到类似以下的输出：
```
## Gemini CLI Information

**Version:**
[版本号]

**Available Models:**
- gemini-2.0-flash-exp (Latest, faster)
- gemini-1.5-pro (Stable, reliable)
- gemini-1.5-flash (Fast, efficient)
...
```

---

### 测试 2: MCP 工具 - 直接询问 Gemini

```
你: 使用 gemini_chat 询问 "什么是闭包？请用简单的例子说明。"
```

**预期结果**：应该收到 Gemini 的回答。

---

### 测试 3: AI 对比 Skill

```
你: 对比 Claude 和 Gemini 对于"如何优化 React 性能"的回答
```

**预期结果**：
1. Claude 先给出分析
2. 自动调用 Gemini 获取回答
3. 提供结构化的对比分析

---

### 测试 4: 多 AI 协作 Agent

```
你: 使用 multi-ai-orchestrator agent 研究微服务架构的优势和挑战，使用 Competitive 模式
```

**预期结果**：
1. Agent 确认使用 Competitive 模式
2. Claude 和 Gemini 各自独立分析
3. 提供对比和综合建议

**注意**：也可以自动触发，描述任务时使用关键词：
```
你: 我需要两个 AI 协作分析这个微服务架构的设计
```

---

## 基础使用示例

### 示例 A: 直接询问 Gemini（最简单）

```
你: 使用 gemini_chat 询问 "什么是 React Server Components？"
```

**何时使用**：
- 需要第二个 AI 的意见
- 想要 Gemini 的长上下文能力
- 获取不同视角

---

### 示例 B: 对比两个 AI（推荐）

```
你: 对比 Claude 和 Gemini 对这个问题的回答：
"使用 TypeScript 的好处是什么？"
```

**自动触发**：使用关键词时，`ai-compare` skill 会自动激活
- "对比"
- "compare"
- "两个 AI 都看看"
- "问问 Gemini 怎么想"

**预期输出格式**：
```markdown
## Claude's Analysis:
[Claude 的回答]

## Gemini's Analysis:
[Gemini 的回答]

## Comparison Summary:
### 相似点：
- ...

### 不同点：
| Aspect | Claude | Gemini |
|--------|--------|--------|
| ...    | ...    | ...    |
```

---

### 示例 C: 使用协作 Agent（复杂任务）

**方式 1：显式调用**
```
你: 使用 multi-ai-orchestrator agent 让两个 AI 协作审查这段代码的安全性
```

**方式 2：自动触发**（使用关键词）
```
你: 让两个 AI 协作完成代码安全审查
你: 使用 Competitive 模式比较两个方案
```

**5 种协作模式**：

| 模式 | 触发方式 | 适用场景 |
|------|---------|---------|
| **Sequential** | "流水线" | 一个 AI 的输出作为下一个的输入 |
| **Parallel** | "并行" | 同时处理不同方面 |
| **Competitive** | "竞争" | 独立解决同一问题，选最佳 |
| **Collaborative** | "协作" | 联合集成方案 |
| **Supervisor-Worker** | "监督" | Claude 编排，Gemini 执行 |

---

## 验证检查清单

在开始使用前，请确认以下所有项：

### ✅ 环境检查

- [ ] Gemini CLI 已安装并可用 (`gemini --version`)
- [ ] Node.js >= 18.0.0 (`node --version`)
- [ ] MCP Server 依赖已安装 (`npm install` 完成)
- [ ] 全局配置文件存在：
  - [ ] `~/.claude/mcp.json`
  - [ ] `~/.claude/skills/ai-compare/SKILL.md`
  - [ ] `~/.claude/agents/multi-ai-orchestrator/AGENT.md`

### ✅ 功能检查

- [ ] **测试 1 通过**：`gemini_model_info` 显示模型信息
- [ ] **测试 2 通过**：`gemini_chat` 可以获取 Gemini 回答
- [ ] **测试 3 通过**：`ai-compare` skill 自动触发
- [ ] **测试 4 通过**：`/multi-ai-orchestrator` agent 正常工作

### ✅ 文件格式检查

根据 Claude Code 官方文档规范：

**Skill 格式**：
```yaml
---
name: ai-compare
description: Compare responses...
allowed-tools: gemini_chat
---
```
- [ ] 使用 `allowed-tools` (Skill 专用字段)
- [ ] name 使用小写字母和连字符
- [ ] description 清楚说明触发条件

**Agent 格式**：
```yaml
---
name: multi-ai-orchestrator
description: Orchestrates complex tasks...
tools: gemini_chat
model: inherit
---
```
- [ ] 使用 `tools` (Agent 专用字段)
- [ ] 包含 `model: inherit`
- [ ] name 使用小写字母和连字符

---

## 常见问题

### Q1: MCP Server 没有加载？

**检查清单：**
1. ✅ 运行了 `npm install` 吗？
2. ✅ `~/.claude/mcp.json` 文件存在吗？
3. ✅ 路径正确吗？（运行 `ls` 验证）
4. ✅ JSON 格式正确吗？（检查逗号、引号）
5. ✅ 重启了 Claude Code 吗？

**调试：**
```bash
# 检查配置文件
cat ~/.claude/mcp.json

# 手动测试 MCP Server
cd /Users/apple/claudecode/claude-gemini-bridge/gemini-mcp-server
npm start
```

---

### Q2: `gemini` 命令失败？

**检查：**
```bash
# Gemini CLI 可用吗？
which gemini
gemini --version

# 手动测试
gemini "test"
```

**如果不可用，安装 Gemini CLI：**
```bash
# 参考 Gemini CLI 官方文档
# https://github.com/google/gemini-cli
```

---

### Q3: Skills/Agents 没有触发？

**确保：**
1. ✅ 使用了触发关键词（对比、ask both、协作等）
2. ✅ 问题描述清晰
3. ✅ MCP Server 已配置并工作
4. ✅ 全局文件存在：
   ```bash
   ls ~/.claude/skills/ai-compare/SKILL.md
   ls ~/.claude/agents/multi-ai-orchestrator/AGENT.md
   ```

**手动触发：**
```
你: 使用 ai-compare skill 对比这两个回答
你: 启动 /multi-ai-orchestrator agent
```

---

### Q4: 代理设置问题？

MCP Server 配置了代理（http://127.0.0.1:7890），如果你的网络环境不同：

**修改 MCP Server 代码**：
```bash
# 编辑文件
vim /Users/apple/claudecode/claude-gemini-bridge/gemini-mcp-server/src/index.js

# 找到这段（第 55-60 行）并修改：
env: {
  ...process.env,
  all_proxy: 'http://127.0.0.1:7890',  // 改为你的代理
  http_proxy: 'http://127.0.0.1:7890',
  https_proxy: 'http://127.0.0.1:7890',
}
```

---

## 下一步

### 📚 学习更多
- 阅读 [README.md](../README.md)
- 查看 [示例 1: 基础使用](./example-1-basic-usage.md)
- 查看 [示例 2: 协作审查](./example-2-orchestration.md)
- 查看 [示例 3: 研究任务](./example-3-research.md)
- 阅读 [Claude Code 官方文档 - Skills](https://code.claude.com/docs/en/skills)
- 阅读 [Claude Code 官方文档 - Subagents](https://code.claude.com/docs/en/sub-agents)

### 🎯 实践建议
1. **从简单开始**：先测试 `gemini_model_info` 和 `gemini_chat`
2. **逐步深入**：尝试 AI 对比功能
3. **复杂任务**：在真实项目中使用协作 agent
4. **自定义优化**：根据你的需求调整 skill 和 agent

### 💡 使用场景

| 场景 | 推荐方式 | 示例 |
|------|---------|------|
| **快速获取第二意见** | `gemini_chat` | "询问 Gemini 如何优化这段代码" |
| **学习对比** | `ai-compare` skill | "对比两个 AI 对闭包的解释" |
| **代码审查** | `/multi-ai-orchestrator` | "使用两个 AI 审查代码安全性" |
| **文档编写** | Collaborative 模式 | "协作编写 API 文档" |
| **架构设计** | Competitive 模式 | "竞争设计架构方案" |
| **问题调试** | Sequential 模式 | "流水线式调试这个 bug" |

---

## 快速参考卡

### MCP 工具（基础层）

```
gemini_chat        - 向 Gemini 提问
gemini_model_info  - 查看 Gemini 模型信息
```

### Skill（知识层）

```
ai-compare         - AI 响应对比技能（自动触发）
触发关键词: "对比", "compare", "两个 AI", "问问 Gemini"
```

### Agent（编排层）

```
multi-ai-orchestrator  - 多 AI 协作器
触发方式: "使用 multi-ai-orchestrator", "协作", "orchestrate"

协作模式:
  Sequential (🔁) - 流水线
  Parallel (⚡) - 并行
  Competitive (🏆) - 竞争
  Collaborative (🤝) - 协作
  Supervisor-Worker (👔) - 监督
```

### 关键词速查

| 关键词 | 触发的组件 | 效果 |
|--------|-----------|------|
| "使用 gemini_chat" | MCP 工具 | 调用 Gemini |
| "对比两个 AI" | ai-compare skill | 结构化对比 |
| "使用 multi-ai-orchestrator" | Agent | 启动协作器 |
| "两个 AI 协作" | Agent (自动) | 自动触发协作 |
| "竞争模式" | Agent → Competitive | 两个 AI 独立解决 |

---

## 架构总结

```
┌─────────────────────────────────────────────────────┐
│  全局配置（已配置）                                   │
│  ~/.claude/                                          │
│  ├── mcp.json → MCP Server                           │
│  ├── skills/ai-compare/ → AI 对比技能                │
│  └── agents/multi-ai-orchestrator/ → 多 AI 编排       │
└─────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────┐
│  三层架构                                            │
│  1. MCP 工具层 → 基础 Gemini 调用                    │
│  2. Skill 层   → 知识和最佳实践                       │
│  3. Agent 层   → 复杂任务编排                         │
└─────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────┐
│  使用方式                                            │
│  - 自动触发：使用关键词（"对比"、"协作"等）            │
│  - 显式调用："使用 multi-ai-orchestrator agent"      │
│  - 工具调用：gemini_chat                              │
└─────────────────────────────────────────────────────┘
```

---

## 获取帮助

遇到问题时：

1. **检查配置** - 使用上面的验证检查清单
2. **查看日志** - Claude Code 终端输出
3. **简化测试** - 从最简单的测试开始
4. **查看文档** - 阅读各组件的 README.md
5. **官方文档** - [Claude Code Docs](https://code.claude.com/docs)

---

**🎉 恭喜！你已经配置好了全局可用的 Claude-Gemini Bridge！**

现在你可以在任何项目中使用多个 AI 协作的力量！

**下一步**：按照上面的"测试步骤"验证配置是否正常工作。
