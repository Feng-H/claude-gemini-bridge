# Claude-Gemini Bridge

让 Claude Code 和 Gemini AI 协同工作的完整工具集。

## 项目概述

本项目提供了三个层面的工具，让你在 Claude Code 中无缝使用 Gemini AI：

1. **MCP Server** - 核心：提供 4 个 Gemini API 访问工具
2. **Skills** - 知识：专业化的 AI 协作最佳实践
3. **Agents** - 编排：复杂的多 AI 任务编排器

## 🆕 v1.1.0 新功能

- ✅ **gemini_analyze** - 大文件和代码库深度分析
- ✅ **init_gemini** - 自动生成 GEMINI.md 项目文档
- ✅ **gemini-analyzer Skill** - 触发式大上下文分析
- ✅ **dual-review Skill** - 双 AI 独立审查机制
- ✅ **init-gemini Agent** - Gemini 项目初始化

**设计理念**：简洁、实用、符合 MCP 最佳实践。无需复杂的 slash commands，通过自然语言即可触发。

## 项目结构

```
claude-gemini-bridge/
├── gemini-mcp-server/          # MCP Server（核心）
│   ├── src/
│   │   └── index.js           # Server 实现（4个工具）
│   ├── package.json            # v1.1.0
│   ├── README.md              # MCP Server 文档
│   └── CLAUDE.md              # Claude Code 使用指南
│
├── skills/                     # Skills（GitHub 包含）
│   ├── ai-compare/            # AI 响应对比
│   ├── gemini-analyzer/       # 🆕 大文件分析
│   └── dual-review/           # 🆕 双 AI 审查
│
├── agents/                     # Agents（GitHub 包含）
│   ├── init-gemini/           # 🆕 项目初始化
│   └── multi-ai-orchestrator/ # 多 AI 协作
│
├── ai-orchestrator/            # 原始本地副本
│   └── examples/              # 使用示例
│
├── ARCHITECTURE.md             # 架构和功能说明
├── INSTALL.md                  # 🆕 安装指南
└── README.md                   # 本文件
```

## 六种使用场景

### 场景 1: 快速对比 AI 响应 🔍

使用 **ai-compare Skill**

```bash
# 在 Claude Code 中
你: "对比 Claude 和 Gemini 对这个问题的回答：如何优化 React 性能？"
```

**适用情况：**
- 需要不同观点
- 验证答案准确性
- 学习不同 AI 的特点

### 场景 2: 直接调用 Gemini 🤖

使用 **MCP Server 工具**

```bash
# 在 Claude Code 中
你: "使用 gemini_chat 工具，询问 '量子计算的最新进展'"
```

**适用情况：**
- 利用 Gemini 的长上下文（最多 100 万 tokens）
- 作为 Claude 的备用
- 获取不同的 AI 视角

**⚠️ 重要说明**:
- `gemini_chat` 调用的是 **Gemini CLI**，不是网页检索工具
- `WebSearch` 是 Claude Code **内置的网页搜索工具**，与 Gemini 无关
- 如果需要 Gemini 的 Google Search 能力，需要在 Gemini CLI 中单独配置
- 本项目的 `gemini_chat` 主要用于获取 Gemini AI 的对话响应，不包含网页检索功能

### 场景 3: 智能项目分析 📊

使用 **gemini-analyzer Skill** 🆕

```bash
# 在 Claude Code 中（自然语言触发）
你: "用 Gemini 分析这个项目"
你: "让 Gemini 审查这个文档项目"
你: "分析这个代码库的架构"
```

**智能检测项目类型：**
- 📚 **文档项目**（如知识库、方法论系统）
  - 自动读取 Markdown 文件
  - 扫描文档目录（_系统/、项目/、docs/ 等）
  - 支持中文文档（项目说明.md）

- 💻 **编程项目**（如 Web 应用、工具库）
  - 读取源代码目录（src/, lib/, components/ 等）
  - 支持 16 种编程语言
  - 分析配置文件

- 🗂️ **混合项目**（如配置管理、数据工程）
  - 读取所有重要文件
  - 自动排除临时文件

**适用情况：**
- 任何类型的项目分析
- 利用 Gemini 的 1M token 上下文
- 自动适配项目结构

### 场景 4: 双 AI 独立审查 🛡️

使用 **dual-review Skill** 🆕

```bash
# 在 Claude Code 中
你: "双 AI 审查这个支付模块"
你: "让两个 AI 分别审查代码，然后汇总"
```

**工作流程：**
1. Claude 独立审查
2. Gemini 独立审查（大上下文）
3. Claude 汇总两个 AI 的发现
4. 提供综合建议

**适用情况：**
- 关键安全代码
- 生产发布前审查
- 高质量要求
- 需要双重验证

### 场景 5: 项目初始化 🚀

使用 **init-gemini Agent** 🆕

```bash
# 在 Claude Code 中
你: "用 Gemini 初始化这个项目"
你: "让 Gemini 创建 GEMINI.md"
```

**生成内容：**
- 项目概览（Gemini 视角）
- 技术栈分析
- 架构说明
- 开发指南
- AI 辅助上下文

**适用情况：**
- 新项目启动
- 现有项目分析
- 需要项目文档
- AI 协作准备

### 场景 6: 复杂多 AI 协作 🤝

使用 **multi-ai-orchestrator Agent**

```bash
# 在 Claude Code 中
你: "使用 multi-ai-orchestrator 让两个 AI 协作完成这个代码审查任务"
```

**适用情况：**
- 多步骤复杂任务
- 需要串行/并行处理
- 大型项目协作

## 快速开始

### 前置要求

1. **已安装 Gemini CLI**
   ```bash
   gemini --version
   ```

2. **配置 Gemini API Key**
   ```bash
   # 根据 Gemini CLI 文档配置
   ```

3. **Node.js >= 18.0.0**

### 安装步骤

#### 1. 安装 MCP Server 依赖

```bash
cd /Users/apple/claudecode/claude-gemini-bridge/gemini-mcp-server
npm install
```

#### 2. 配置 MCP Server

编辑 `~/.claude/mcp.json`（如果不存在则创建）：

```json
{
  "mcpServers": {
    "gemini": {
      "command": "node",
      "args": [
        "/Users/apple/claudecode/claude-gemini-bridge/gemini-mcp-server/src/index.js"
      ]
    }
  }
}
```

**环境变量配置（可选）**：

如果需要使用代理，设置环境变量：

```bash
export HTTPS_PROXY=http://127.0.0.1:7890
export HTTP_PROXY=http://127.0.0.1:7890
```

#### 3. 配置 Skills 和 Agents

Skills 和 Agents 会自动从 `ai-orchestrator/.claude/` 目录加载。

如需在其他项目中使用，创建符号链接：

```bash
# 在其他项目中
ln -s /Users/apple/claudecode/claude-gemini-bridge/ai-orchestrator/.claude .claude
```

#### 4. 重启 Claude Code

配置完成后重启 Claude Code。

## 使用示例

### 示例 1: 简单查询

```bash
# Claude Code 中
你: 使用 gemini_chat 询问 "什么是函数式编程？"

# Claude 会调用 Gemini CLI 并返回结果
```

### 示例 2: AI 响应对比

```bash
你: 对比 Claude 和 Gemini 对这个问题的回答：
"TypeScript 和 JavaScript 的区别是什么？"

# Claude 会：
# 1. 提供自己的回答
# 2. 调用 gemini_chat 获取 Gemini 的回答
# 3. 提供结构化的对比分析
```

### 示例 3: 多 AI 协作代码审查

```bash
你: /multi-ai-orchestrator

# Agent 启动后：
你: 让两个 AI 协作审查这个 React 组件的代码

# Agent 会：
# 1. Claude: 分析安全性、性能
# 2. Gemini: 分析最佳实践、可读性
# 3. 整合发现并提供建议
```

### 示例 4: 并行处理

```bash
你: /multi-ai-orchestrator

你: 让两个 AI 并行工作：
- Claude: 写这个功能的技术文档
- Gemini: 写用户友好的使用指南

# Agent 会并行分配任务并整合结果
```

## MCP Server 工具参考

### gemini_chat

发送提示词到 Gemini。

**参数：**
- `prompt` (必需): 提示词
- `model` (可选): 模型名称（**不推荐指定**，让 Gemini CLI 自动选择默认模型）

**⚠️ 重要提示**:
- **不要指定 model 参数**，让系统自动选择默认模型
- 指定具体模型可能导致 "Requested entity was not found" 错误
- Gemini CLI 会根据配置自动选择最合适的模型

**示例：**
```javascript
// ✅ 推荐：不指定模型
{
  "prompt": "解释 React Hooks"
}

// ❌ 不推荐：指定模型（可能导致调用失败）
{
  "prompt": "解释 React Hooks",
  "model": "gemini-2.0-flash-exp"
}
```

### gemini_model_info

获取 Gemini CLI 和模型信息。

### gemini_analyze 🆕

使用 Gemini 的大上下文能力进行智能项目分析。

**智能检测项目类型：**
- 📚 **文档项目**: Markdown > 5 且代码 < 3
- 💻 **编程项目**: 代码文件 > 5
- 🗂️ **混合项目**: 其他情况

**参数：**
- `target` (必需): 文件路径、目录路径或代码内容
- `analysis_type` (可选): 分析类型
  - `security` - 安全漏洞分析
  - `performance` - 性能瓶颈分析
  - `architecture` - 架构审查
  - `bugs` - Bug 检测
  - `review` - 全面项目审查
  - `general` - 综合分析（默认）
- `model` (可选): Gemini 模型名称（**不推荐指定**）

**支持的文件类型：**
- 代码: .js, .ts, .jsx, .tsx, .py, .java, .go, .rs, .cpp, .c, .cs, .sh, .bash, .ps1, .php, .rb 等
- 文档: .md（包括中文文档：项目说明.md）
- 配置: .json, .yaml, .yml, .xml, .toml, .ini, .conf, .env
- 数据: .csv, .tsv, .txt, .log

**支持的目录：**
- 文档: _系统/, 项目/, docs/, 文档/, 指南/
- 代码: src/, lib/, app/, server/, components/, handlers/, utils/, services/

**示例：**
```javascript
// 文档项目
{
  "target": "~/claudecode/engineer-method",
  "analysis_type": "review"
}

// 编程项目
{
  "target": "./src",
  "analysis_type": "security"
}
```

### init_gemini 🆕

初始化项目并生成 GEMINI.md 文档。

**参数：**
- `project_path` (可选): 项目路径，默认当前目录
- `model` (可选): Gemini 模型名称（**不推荐指定**）

**示例：**
```javascript
{
  "project_path": "."
}
```

**生成内容：**
- 项目概览（Gemini 视角）
- 技术栈分析
- 架构说明
- 开发指南
- AI 辅助上下文

## 协作模式参考

### Sequential (串行) 🔁
一个 AI 的输出作为下一个的输入。

**适用：** 多阶段工作流、渐进式改进

### Parallel (并行) ⚡
两个 AI 同时处理不同方面。

**适用：** 时间敏感任务、独立子任务

### Competitive (竞争) 🏆
两个 AI 独立解决同一问题。

**适用：** 探索不同方法、寻找最优解

### Collaborative (协作) 🤝
两个 AI 共同创建集成解决方案。

**适用：** 复杂问题解决、创造性任务

### Supervisor-Worker (分层) 👔
Claude 编排，Gemini 执行子任务。

**适用：** 大型项目管理、任务委派

## 最佳实践

### 1. 选择合适的方式

```
简单查询         → MCP Server (gemini_chat)
快速对比         → Skill (ai-compare)
复杂协作任务     → Agent (multi-ai-orchestrator)
```

### 2. 明确目标

告诉 AI 你想要什么：
- ✅ "对比技术准确性"
- ✅ "提供不同观点"
- ✅ "并行处理这两个任务"
- ❌ "看看 Gemini 怎么说"

### 3. 利用各自优势

**Claude 擅长：**
- 技术准确性
- 代码分析
- 上下文理解
- 逻辑推理

**Gemini 擅长：**
- 创意内容
- 长上下文处理
- 多样化视角
- 多模态能力

### 4. 管理上下文

Gemini 不了解当前对话，提供必要背景：
```javascript
{
  "prompt": "基于以下代码：[code]，解释..."
}
```

### 5. 验证和整合

- 交叉验证两个 AI 的结果
- 整合最佳观点
- 注明不一致之处
- 提供综合建议

## 故障排除

### MCP Server 未加载

```bash
# 检查配置
cat ~/.claude/mcp.json

# 检查文件权限
ls -la gemini-mcp-server/src/index.js

# 测试 MCP Server
cd gemini-mcp-server && npm start
```

### Gemini CLI 错误

```bash
# 检查 Gemini CLI
gemini --version

# 重新配置 API Key
# (参考 Gemini CLI 文档)
```

### Skill/Agent 未触发

- 确保关键词匹配 description
- 尝试更明确的请求
- 检查 `.claude/` 目录路径

## 进阶使用

### 自定义协作模式

在 `ai-orchestrator/.claude/agents/` 下创建新 Agent：

```markdown
---
name: my-custom-orchestrator
description: My custom multi-AI workflow
allowed-tools: gemini_chat
---

# Custom Agent Content
```

### 扩展 MCP Server

编辑 `gemini-mcp-server/src/index.js` 添加新工具。

### 组合使用

```bash
# 先用 Skill 对比
你: "对比这两个 AI 对 X 的回答"

# 然后用 Agent 深入协作
你: "/multi-ai-orchestrator 让它们协作改进方案"
```

## 项目维护

### 更新依赖

```bash
cd gemini-mcp-server
npm update
```

### 查看日志

MCP Server 日志输出到 stderr：
```bash
# 在启动 Claude Code 的终端查看
```

### 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 相关资源

- [Claude Code 文档](https://code.claude.com/docs)
- [MCP 协议](https://modelcontextprotocol.io)
- [Gemini CLI 文档](https://github.com/google/gemini-cli)
- [项目示例](./examples/)

## 常见问题

**Q: 为什么有时用 Claude，有时用 Gemini？**

A: 各有优势。Claude 擅长技术准确性和上下文理解，Gemini 擅长创意和长上下文。本项目让你灵活选择或组合使用。

**Q: MCP Server、Skill、Agent 有什么区别？**

A:
- **MCP Server**: 提供工具访问能力
- **Skill**: 提供知识和最佳实践
- **Agent**: 执行复杂的多步骤任务

**Q: 可以在多个项目中使用吗？**

A: 可以！MCP Server 全局配置一次，Skills 和 Agents 可以符号链接或复制到多个项目。

**Q: 性能如何？**

A: Gemini CLI 调用通常 1-5 秒。建议先让 Claude 回答，再并行调用 Gemini 对比。

---

**Created with ❤️ for multi-AI collaboration**
